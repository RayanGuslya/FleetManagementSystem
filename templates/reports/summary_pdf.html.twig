<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <style>
        body { font-family: 'DejaVu Sans', sans-serif; font-size: 12px; }
        table { width: 100%; border-collapse: collapse; margin-top: 20px; }
        th, td { border: 1px solid #000; padding: 8px; text-align: center; }
        th { background-color: #f0f0f0; }
        h2 { text-align: center; }
    </style>
</head>
<body>
    <h2>Сводный отчёт</h2>
    <table>
        <thead>
            <tr>
                <th>ID авто</th>
                <th>Модель</th>
                <th>Пробег (км)</th>
                <th>Топливо (л)</th>
                <th>Км/л</th>
            </tr>
        </thead>
        <tbody>
            {% for r in rows %}
            <tr>
                <td>{{ r.vehicle_id }}</td>
                <td>{{ r.model }}</td>
                <td>{{ r.total_km|number_format(0, '', ' ') }}</td>
                <td>{{ r.total_fuel|number_format(1, ',', ' ') }}</td>
                <td>{{ (r.total_km / (r.total_fuel ?: 1))|round(2) }}</td>
            </tr>
            {% else %}
            <tr><td colspan="5">Нет данных</td></tr>
            {% endfor %}
        </tbody>
    </table>
</body>
</html>